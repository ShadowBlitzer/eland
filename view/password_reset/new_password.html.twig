{% extends 'anon.html.twig' %}

{% import 'macro/button.html.twig' as b %}
{% import 'macro/panel.html.twig' as p %}
{% import 'macro/asset.html.twig' as a %}

{% block js -%}
{{- a.js('generate_password.js') -}}
{%- endblock js %}

{% block fa -%}key{%- endblock %}
{% block heading -%}{{- 'new_password.title'|trans -}}{%- endblock heading %}

{% block inner -%}

<p>{{- 'new_password.explain'|trans -}}</p>

{{- p.form_start(form) -}}
{{- form_row(form.password, {'addon_class': 'addon-info', 'addon_btn_class': 'btn-info'}) -}}
{{- b.info(form.submit, 'password_reset.submit_btn') -}}
{{- p.form_end(form) -}}

{%- endblock inner %}



{#


{% block content %}

<div class="row"><div class="col-md-12">
<p></p>

<h1>{{ 'new_password.heading'|trans }}</h1>

{{ form_start(form) }}

<p>

<div class="input-group input-group-lg">
  <span class="input-group-btn">
	<button class="btn btn-info" type="button"><i class="fa fa-key"></i></button>
  </span>
{{ form_widget(form.password, {'attr': {'placeholder': 'new_password.password' }}) }}
  <span class="input-group-btn">
	<button class="btn btn-info" type="button" id="generate">{{ 'new_password.generate_btn'|trans }}</button>
  </span>

</div>
{{ form_errors(form.password) }}
<p>{{ 'new_password.password_explain'|trans }}</p>
</p>

<p>
{{ form_widget(form.submit, {'label': 'new_password.submit_btn', 'attr': {'class': 'btn btn-default btn-lg'}}) }}
</p>

{{ form_end(form) }}

</div>
</div>

{% endblock content %}

{% block js %}
<script src="{{ asset('generate_password.js') }}"></script>
{% endblock js %}

#}

